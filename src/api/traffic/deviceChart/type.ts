export interface formatData {
    baseDate: string
    dayStep: string
    adx: string
    os: string
    country: string
  }
  
 
export interface paramsData {
    countryos: string; // JSON 字符串化的对象
    days: string; // 计算得到的天数
  }
  
  